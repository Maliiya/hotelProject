.search-result__options
  .search-result__options-content-zone
    form.search-result__options-form
      label.search-field__text даты пребывания в отеле
        select(type="date").search-field__input.search-result__options-input
      label.search-field__text гости
        select(type="number").search-field__input
          option(disabled) Взрослые
            option(type="number" novalidate) 1
          option(disabled) Дети
            option(type="number") 1
          option(disabled) Младенцы
            option(type="number" novalidate) 1
      label.search-field__text диапазон цен
      p.search-field__text.search-field__text-side 5 000₽ - 10 000₽
        input(type="range" min="1" max="100" value="50").search-field__input.slider
      p.search-field__text-side Стоимость за сутки пребывания в номере
      label.search-field__text Checkbox Buttons
        label Можно курить
          input(type="checkbox").search-field__input-checkbox
        label Можно с питомцами
          input(type="checkbox").search-field__input-checkbox
        label Можно пригласить гостей (до 10 человек)
          input(type="checkbox").search-field__input-checkbox
      label.search-field__text доступность
        label.search-field__text Широкий коридор
        label.search-field__text-side Ширина коридоров в номере не менее 91 см.
          input(type="checkbox").search-field__input-checkbox
        label.search-field__text Помощник для инвалидов
        label.search-field__text-side На 1 этаже вас встретит специалист  и проводит до номера.
          input(type="checkbox").search-field__input-checkbox
      label.search-field__text удобства номера
        select(type="number").search-field__input
          option(disabled) Взрослые
            option(type="number" novalidate) 1
          option(disabled) Дети
            option(type="number") 1
          option(disabled) Младенцы
            option(type="number" novalidate) 1
      label.search-field__text дополнительные удобства
        select(type="number").search-field__input
          option(disabled) Взрослые